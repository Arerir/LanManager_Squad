# Orchestration Log: Merlin (Backend Dev)

**Date:** 2026-04-07T15:26:09  
**Task:** tank-signalr-doorscan  
**Status:** ✅ Complete

## Summary

Added SignalR broadcast for door scan events. Backend now broadcasts "UserDoorScanned" to all connected clients after every door pass is recorded.

## Changes Made

- **Modified** `AttendanceBroadcast.cs` — added `DoorScanBroadcast` record with fields: EventId, UserId, UserName, Direction, ScannedAt
- **Modified** `DoorPassController.cs` — injected `IHubContext<AttendanceHub>` via primary constructor
- **Added** broadcast call in `DoorScan` endpoint after `db.SaveChangesAsync()` — broadcasts to all clients on channel `"UserDoorScanned"`

## Implementation Details

- **Event Name:** `UserDoorScanned`
- **Hub:** `/hubs/attendance` (same hub as UserCheckedIn)
- **Broadcast scope:** All connected clients
- **Payload:** DoorScanBroadcast (camelCase JSON via System.Text.Json)

## Verification

- Build clean ✓
- No compilation errors ✓
- Follows existing CheckInController pattern ✓

## Next Steps

- Circe/Switch to implement MAUI listener via SignalRService

